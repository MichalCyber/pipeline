version: "3.9"

services:
  api:
    build: .
    container_name: viral-pipeline-api
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - ./outputs:/app/outputs
      - ./logs:/app/logs
    restart: unless-stopped
